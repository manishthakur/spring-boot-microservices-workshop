app:
  name: My App
  description: Welcome to ${app.name}

my:
  greeting: Hello
  list:
    values: One, Two, Three, Four

db:
  connection: "{title: 'Sample Konfabulator Widget', name: 'main_window', width: 500, height: 500}"
  host: 127.0.0.1
  port: 1200

management.endpoints.web.exposure.include: "*"
spring:
  profiles:
    active: test